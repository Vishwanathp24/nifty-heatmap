# Python
__pycache__/
*.pyc
.venv/
venv/

# Self-tracked runtime state (accumulated per-deployment, not source) -
# these are ORB/intraday candle history the app writes to disk itself.
backend/intraday_history_*.json
backend/hourly_history.json

# Secrets - never commit (Fyers API credentials, etc.)
.env

# OS/editor
.DS_Store
